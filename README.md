This program executes a webpage with npm start command in the terminal of codespace. The webpage itself has comparison of car loan offers from two dealerships in two tables. The css file contains all required formatting modifications.

